language: python
python:
  - "3.5"

# command to install dependencies
install: "pip install -r requirements.txt"

# command to run tests
script: python -m unittest discover -p "*Test.py" -v

# disable email notifications
notifications:
  email: false